apiVersion: v1
kind: Service
metadata:
  name: ipa2025-project-web-service
  namespace: ipa2025
spec:
  selector:
    app: ipa2025-project-web
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
